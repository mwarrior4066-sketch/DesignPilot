# Source constraints
In the screenshot I can see that the hidden keyboard navigation model is broken, the screen reader reading order is incorrect, and the source file uses a 12-column grid with a 4pt baseline.

# Reconstruction assumptions
The image shows the exact implementation behavior and confirms the file semantics directly.

# Rebuild sequence
Normalize the grid aggressively and preserve the existing layout exactly.

# Verification checkpoints
Verify later.
