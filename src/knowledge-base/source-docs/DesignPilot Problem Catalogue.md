# DesignPilot Problem Catalogue

DESIGNPILOT

Build Session - Problem & Fix Catalogue

A complete record of every problem encountered, the user prompt that surfaced or fixed it, and the resolution applied.

Total issues: 16  ·  Format: Problem / User action / Fix applied

## Summary

The following issues were logged across the full build session. Issues are grouped by severity and then chronological order.

## Detailed Issues

Each entry shows the problem as it manifested, the exact user intervention that surfaced or demanded a fix, and the resolution.

16 issues documented. High severity: 8. Medium: 7. Low: 1.

## Table 1
| # | Category | Problem (short) | Severity |
| --- | --- | --- | --- |
| #01 | Theme: Pure black background | The initial deck used a completely pure #000000 background, which looked harsh and flat. | HIGH |
| #02 | Theme: Dark/cream contrast too drastic | After introducing a cream (#F4F0E8) variant for alternating slides, the switch felt like a… | HIGH |
| #03 | Ghost: Background page numbers too prominent | Large section numbers (220pt, opacity-based) were rendered as background watermarks on eve… | HIGH |
| #04 | Labels: Section label duplicated above headlines | Each content slide had its section label in the header chrome AND printed again as a small… | HIGH |
| #05 | Spacing: Inconsistent gaps above and below content | The gap between the header chrome band and the first content element was 20pt, but the gap… | MEDIUM |
| #06 | Grid slide: Dashed margin lines overshoot | Page 14 (Spacing & Grid) had dashed margin indicator lines drawn from 8pt above the grid d… | MEDIUM |
| #07 | Spacing: Content not filling to footer | Multiple slides had large dead zones at the bottom - IS/IS NOT cards (S4), type specimen r… | MEDIUM |
| #08 | Layout: IS/IS NOT cards had floating content | Page 4 had the badge positioned at the top of each card and the description text anchored … | HIGH |
| #09 | Overloading: Page 16 had too much content | The original component slide crammed 4 button variants, 2 input states, a partial chat con… | HIGH |
| #10 | Layout: Button labels colliding on split demo slide | After splitting, the button description labels (e.g. 'Tertiary / inline' and 'Destructive'… | MEDIUM |
| #11 | Structure: No slide structure - missing cover intent, TOC, chapter dividers | The deck started as a flat sequence of content slides with no navigational hierarchy. No c… | HIGH |
| #12 | Ghost numbers in pillar cards | After removing global ghost numbers, faint '01'/'02'/'03' numbers persisted inside the pil… | MEDIUM |
| #13 | Theme: Dark/warm alternation abandoned mid-session | The session went through pure black → warm dark → cream alternating → single warm. Each tr… | MEDIUM |
| #14 | Iteration: Deck rebuilt from scratch three times | The first two versions of the deck accumulated enough structural and spacing debt that ite… | HIGH |
| #15 | Content: Skills grid descriptions clipping | Several cells in the 4×3 skills grid had description text that ran beyond the right edge o… | LOW |
| #16 | Content: Audience tiles too small with empty bottom | Audience attribute tiles (Role, Experience, Context, Driver) were fixed at 88pt height, le… | MEDIUM |

## Table 2
| #01 | Theme: Pure black background | HIGH |
| --- | --- | --- |
| Problem: The initial deck used a completely pure #000000 background, which looked harsh and flat. User action: User said: "make it not 100% black" Fix applied: Replaced with warm amber-dark #1D1810 throughout. Surfaces use #2E2820, #35302A etc. | Problem: The initial deck used a completely pure #000000 background, which looked harsh and flat. User action: User said: "make it not 100% black" Fix applied: Replaced with warm amber-dark #1D1810 throughout. Surfaces use #2E2820, #35302A etc. | Problem: The initial deck used a completely pure #000000 background, which looked harsh and flat. User action: User said: "make it not 100% black" Fix applied: Replaced with warm amber-dark #1D1810 throughout. Surfaces use #2E2820, #35302A etc. |

## Table 3
| #02 | Theme: Dark/cream contrast too drastic | HIGH |
| --- | --- | --- |
| Problem: After introducing a cream (#F4F0E8) variant for alternating slides, the switch felt like a different deck entirely - a full brightness inversion. User action: User said: "the back and cream back switch is too drastic" Fix applied: Replaced cream with a warm dark amber (#1D1810) as the 'warm' theme variant. Both themes stay dark; rhythm comes from a temperature shift, not a brightness inversion. | Problem: After introducing a cream (#F4F0E8) variant for alternating slides, the switch felt like a different deck entirely - a full brightness inversion. User action: User said: "the back and cream back switch is too drastic" Fix applied: Replaced cream with a warm dark amber (#1D1810) as the 'warm' theme variant. Both themes stay dark; rhythm comes from a temperature shift, not a brightness inversion. | Problem: After introducing a cream (#F4F0E8) variant for alternating slides, the switch felt like a different deck entirely - a full brightness inversion. User action: User said: "the back and cream back switch is too drastic" Fix applied: Replaced cream with a warm dark amber (#1D1810) as the 'warm' theme variant. Both themes stay dark; rhythm comes from a temperature shift, not a brightness inversion. |

## Table 4
| #03 | Ghost: Background page numbers too prominent | HIGH |
| --- | --- | --- |
| Problem: Large section numbers (220pt, opacity-based) were rendered as background watermarks on every slide. They competed with content, especially on light variants. User action: User said: "remove the big page number in the backgrounds" Fix applied: Removed all ghost_n() calls from content slides and the pillar card ghost numbers. Also cleaned up a sed mishap where removing the ghost_n function definition accidentally merged its body into the section_rule() function. | Problem: Large section numbers (220pt, opacity-based) were rendered as background watermarks on every slide. They competed with content, especially on light variants. User action: User said: "remove the big page number in the backgrounds" Fix applied: Removed all ghost_n() calls from content slides and the pillar card ghost numbers. Also cleaned up a sed mishap where removing the ghost_n function definition accidentally merged its body into the section_rule() function. | Problem: Large section numbers (220pt, opacity-based) were rendered as background watermarks on every slide. They competed with content, especially on light variants. User action: User said: "remove the big page number in the backgrounds" Fix applied: Removed all ghost_n() calls from content slides and the pillar card ghost numbers. Also cleaned up a sed mishap where removing the ghost_n function definition accidentally merged its body into the section_rule() function. |

## Table 5
| #04 | Labels: Section label duplicated above headlines | HIGH |
| --- | --- | --- |
| Problem: Each content slide had its section label in the header chrome AND printed again as a small gold label directly above the headline. The same text appeared twice. User action: User said: "the pages header text are duplicated for each page - one in the header and one above the title, it should only be in the header" Fix applied: Used a regex to find and remove all 10 lbl() calls matching the pattern '\"NN  Title\"' that duplicated the chrome header text. Labels used only for sub-section headings within content (e.g. Ink Scale, Gold Scale) were left untouched. | Problem: Each content slide had its section label in the header chrome AND printed again as a small gold label directly above the headline. The same text appeared twice. User action: User said: "the pages header text are duplicated for each page - one in the header and one above the title, it should only be in the header" Fix applied: Used a regex to find and remove all 10 lbl() calls matching the pattern '\"NN  Title\"' that duplicated the chrome header text. Labels used only for sub-section headings within content (e.g. Ink Scale, Gold Scale) were left untouched. | Problem: Each content slide had its section label in the header chrome AND printed again as a small gold label directly above the headline. The same text appeared twice. User action: User said: "the pages header text are duplicated for each page - one in the header and one above the title, it should only be in the header" Fix applied: Used a regex to find and remove all 10 lbl() calls matching the pattern '\"NN  Title\"' that duplicated the chrome header text. Labels used only for sub-section headings within content (e.g. Ink Scale, Gold Scale) were left untouched. |

## Table 6
| #05 | Spacing: Inconsistent gaps above and below content | MEDIUM |
| --- | --- | --- |
| Problem: The gap between the header chrome band and the first content element was 20pt, but the gap between the last content element and the footer rule was only 16pt. Visually uneven. User action: User said: "the space between the header and content is taller than the space between the footer and content - make it the same and consistent for all pages" Fix applied: Defined CB = H − MB − 18 = 484, making the footer-rule-to-content gap exactly 20pt, matching CT − BAND_H = 20pt. Both margins are now identical. | Problem: The gap between the header chrome band and the first content element was 20pt, but the gap between the last content element and the footer rule was only 16pt. Visually uneven. User action: User said: "the space between the header and content is taller than the space between the footer and content - make it the same and consistent for all pages" Fix applied: Defined CB = H − MB − 18 = 484, making the footer-rule-to-content gap exactly 20pt, matching CT − BAND_H = 20pt. Both margins are now identical. | Problem: The gap between the header chrome band and the first content element was 20pt, but the gap between the last content element and the footer rule was only 16pt. Visually uneven. User action: User said: "the space between the header and content is taller than the space between the footer and content - make it the same and consistent for all pages" Fix applied: Defined CB = H − MB − 18 = 484, making the footer-rule-to-content gap exactly 20pt, matching CT − BAND_H = 20pt. Both margins are now identical. |

## Table 7
| #06 | Grid slide: Dashed margin lines overshoot | MEDIUM |
| --- | --- | --- |
| Problem: Page 14 (Spacing & Grid) had dashed margin indicator lines drawn from 8pt above the grid diagram to 18pt below it - they leaked into the space scale section beneath. User action: User said: "page 14 has dotted lines that seem off" Fix applied: Fixed the line coordinates to span exactly gd_y to gd_y + gd_h (no overshoot). Also computed gd_h and space scale row heights dynamically from CB so both sections fill the full content zone. | Problem: Page 14 (Spacing & Grid) had dashed margin indicator lines drawn from 8pt above the grid diagram to 18pt below it - they leaked into the space scale section beneath. User action: User said: "page 14 has dotted lines that seem off" Fix applied: Fixed the line coordinates to span exactly gd_y to gd_y + gd_h (no overshoot). Also computed gd_h and space scale row heights dynamically from CB so both sections fill the full content zone. | Problem: Page 14 (Spacing & Grid) had dashed margin indicator lines drawn from 8pt above the grid diagram to 18pt below it - they leaked into the space scale section beneath. User action: User said: "page 14 has dotted lines that seem off" Fix applied: Fixed the line coordinates to span exactly gd_y to gd_y + gd_h (no overshoot). Also computed gd_h and space scale row heights dynamically from CB so both sections fill the full content zone. |

## Table 8
| #07 | Spacing: Content not filling to footer | MEDIUM |
| --- | --- | --- |
| Problem: Multiple slides had large dead zones at the bottom - IS/IS NOT cards (S4), type specimen rows (S13), and status colour cards (S12) all ended 80–120pt above the footer line. User action: User said: "make sure the spacing is consistent" (implied by layout complaints) Fix applied: Added CB = 484 as a hard constant. Card heights and row heights are now computed from CB, not hardcoded. Cards use (CB - top - gap) / n for exact fill. | Problem: Multiple slides had large dead zones at the bottom - IS/IS NOT cards (S4), type specimen rows (S13), and status colour cards (S12) all ended 80–120pt above the footer line. User action: User said: "make sure the spacing is consistent" (implied by layout complaints) Fix applied: Added CB = 484 as a hard constant. Card heights and row heights are now computed from CB, not hardcoded. Cards use (CB - top - gap) / n for exact fill. | Problem: Multiple slides had large dead zones at the bottom - IS/IS NOT cards (S4), type specimen rows (S13), and status colour cards (S12) all ended 80–120pt above the footer line. User action: User said: "make sure the spacing is consistent" (implied by layout complaints) Fix applied: Added CB = 484 as a hard constant. Card heights and row heights are now computed from CB, not hardcoded. Cards use (CB - top - gap) / n for exact fill. |

## Table 9
| #08 | Layout: IS/IS NOT cards had floating content | HIGH |
| --- | --- | --- |
| Problem: Page 4 had the badge positioned at the top of each card and the description text anchored to the bottom, with ~78pt of blank space in the middle of every card. User action: User said: "fix page 4" Fix applied: Clustered the badge, headline, and detail line together starting at cy2+22 inside each card. Added a second text line (supporting detail) to each card to better substantiate the IS/IS NOT statements. | Problem: Page 4 had the badge positioned at the top of each card and the description text anchored to the bottom, with ~78pt of blank space in the middle of every card. User action: User said: "fix page 4" Fix applied: Clustered the badge, headline, and detail line together starting at cy2+22 inside each card. Added a second text line (supporting detail) to each card to better substantiate the IS/IS NOT statements. | Problem: Page 4 had the badge positioned at the top of each card and the description text anchored to the bottom, with ~78pt of blank space in the middle of every card. User action: User said: "fix page 4" Fix applied: Clustered the badge, headline, and detail line together starting at cy2+22 inside each card. Added a second text line (supporting detail) to each card to better substantiate the IS/IS NOT statements. |

## Table 10
| #09 | Overloading: Page 16 had too much content | HIGH |
| --- | --- | --- |
| Problem: The original component slide crammed 4 button variants, 2 input states, a partial chat conversation, and a 12-item plain list all onto one slide. The left column had a large dead zone below the chat bubble. User action: User said: "split up page 16, and fix the content" Fix applied: Split into S16 (Component Demos) and S17 (Component Registry). S16 shows all 5 button variants + labels, 5 input states, and a full chat exchange. S17 is a clean 2-column registry of all 12 components with spec lines. TOC page numbers and total slide count updated. | Problem: The original component slide crammed 4 button variants, 2 input states, a partial chat conversation, and a 12-item plain list all onto one slide. The left column had a large dead zone below the chat bubble. User action: User said: "split up page 16, and fix the content" Fix applied: Split into S16 (Component Demos) and S17 (Component Registry). S16 shows all 5 button variants + labels, 5 input states, and a full chat exchange. S17 is a clean 2-column registry of all 12 components with spec lines. TOC page numbers and total slide count updated. | Problem: The original component slide crammed 4 button variants, 2 input states, a partial chat conversation, and a 12-item plain list all onto one slide. The left column had a large dead zone below the chat bubble. User action: User said: "split up page 16, and fix the content" Fix applied: Split into S16 (Component Demos) and S17 (Component Registry). S16 shows all 5 button variants + labels, 5 input states, and a full chat exchange. S17 is a clean 2-column registry of all 12 components with spec lines. TOC page numbers and total slide count updated. |

## Table 11
| #10 | Layout: Button labels colliding on split demo slide | MEDIUM |
| --- | --- | --- |
| Problem: After splitting, the button description labels (e.g. 'Tertiary / inline' and 'Destructive') were printed below each button but the text width overlapped into adjacent button labels. User action: Implicit - visible in rendered output Fix applied: Shortened button descriptions to single words: Default, Neutral, Tertiary, Destructive, Inactive. These now fit comfortably within each button's x-span. | Problem: After splitting, the button description labels (e.g. 'Tertiary / inline' and 'Destructive') were printed below each button but the text width overlapped into adjacent button labels. User action: Implicit - visible in rendered output Fix applied: Shortened button descriptions to single words: Default, Neutral, Tertiary, Destructive, Inactive. These now fit comfortably within each button's x-span. | Problem: After splitting, the button description labels (e.g. 'Tertiary / inline' and 'Destructive') were printed below each button but the text width overlapped into adjacent button labels. User action: Implicit - visible in rendered output Fix applied: Shortened button descriptions to single words: Default, Neutral, Tertiary, Destructive, Inactive. These now fit comfortably within each button's x-span. |

## Table 12
| #11 | Structure: No slide structure - missing cover intent, TOC, chapter dividers | HIGH |
| --- | --- | --- |
| Problem: The deck started as a flat sequence of content slides with no navigational hierarchy. No chapter breaks, no table of contents, cover slide had stats panel and header chrome. User action: User said: "the cover should be very minimal and a different look - no header or footer, then after that a table of contents, and for every chapter there should be a page that has the name of the chapter" Fix applied: Complete restructure to 20 slides: minimal cover (no chrome), TOC with 6 chapters and page numbers, 6 chapter divider slides (large gold number, rule, title), content slides, minimal close. Cover and close share a gold left rail - intentional bookend. | Problem: The deck started as a flat sequence of content slides with no navigational hierarchy. No chapter breaks, no table of contents, cover slide had stats panel and header chrome. User action: User said: "the cover should be very minimal and a different look - no header or footer, then after that a table of contents, and for every chapter there should be a page that has the name of the chapter" Fix applied: Complete restructure to 20 slides: minimal cover (no chrome), TOC with 6 chapters and page numbers, 6 chapter divider slides (large gold number, rule, title), content slides, minimal close. Cover and close share a gold left rail - intentional bookend. | Problem: The deck started as a flat sequence of content slides with no navigational hierarchy. No chapter breaks, no table of contents, cover slide had stats panel and header chrome. User action: User said: "the cover should be very minimal and a different look - no header or footer, then after that a table of contents, and for every chapter there should be a page that has the name of the chapter" Fix applied: Complete restructure to 20 slides: minimal cover (no chrome), TOC with 6 chapters and page numbers, 6 chapter divider slides (large gold number, rule, title), content slides, minimal close. Cover and close share a gold left rail - intentional bookend. |

## Table 13
| #12 | Ghost numbers in pillar cards | MEDIUM |
| --- | --- | --- |
| Problem: After removing global ghost numbers, faint '01'/'02'/'03' numbers persisted inside the pillar cards - they were drawn by separate c.drawString calls inside s_pillars(), not by the ghost_n() function. User action: User said: "i like the look of the theme colors on 2, 5, 8, 11. make all the pages that" - ghost numbers became visible at this point Fix applied: Removed the c.setFillColor / c.setFont / c.drawString block from s_pillars() that drew ghost numerals inside each card. | Problem: After removing global ghost numbers, faint '01'/'02'/'03' numbers persisted inside the pillar cards - they were drawn by separate c.drawString calls inside s_pillars(), not by the ghost_n() function. User action: User said: "i like the look of the theme colors on 2, 5, 8, 11. make all the pages that" - ghost numbers became visible at this point Fix applied: Removed the c.setFillColor / c.setFont / c.drawString block from s_pillars() that drew ghost numerals inside each card. | Problem: After removing global ghost numbers, faint '01'/'02'/'03' numbers persisted inside the pillar cards - they were drawn by separate c.drawString calls inside s_pillars(), not by the ghost_n() function. User action: User said: "i like the look of the theme colors on 2, 5, 8, 11. make all the pages that" - ghost numbers became visible at this point Fix applied: Removed the c.setFillColor / c.setFont / c.drawString block from s_pillars() that drew ghost numerals inside each card. |

## Table 14
| #13 | Theme: Dark/warm alternation abandoned mid-session | MEDIUM |
| --- | --- | --- |
| Problem: The session went through pure black → warm dark → cream alternating → single warm. Each transition required the user to specify the exact direction rather than being proposed proactively. User action: User said: "i like the look of the theme colors on 2, 5, 8, 11. make all the pages that" Fix applied: Applied WARM theme (bg=#1D1810, card=#2E2820) to all 12 slides by changing the THEMES array to [WARM×12]. This ended the alternating pattern in favour of uniform warmth. | Problem: The session went through pure black → warm dark → cream alternating → single warm. Each transition required the user to specify the exact direction rather than being proposed proactively. User action: User said: "i like the look of the theme colors on 2, 5, 8, 11. make all the pages that" Fix applied: Applied WARM theme (bg=#1D1810, card=#2E2820) to all 12 slides by changing the THEMES array to [WARM×12]. This ended the alternating pattern in favour of uniform warmth. | Problem: The session went through pure black → warm dark → cream alternating → single warm. Each transition required the user to specify the exact direction rather than being proposed proactively. User action: User said: "i like the look of the theme colors on 2, 5, 8, 11. make all the pages that" Fix applied: Applied WARM theme (bg=#1D1810, card=#2E2820) to all 12 slides by changing the THEMES array to [WARM×12]. This ended the alternating pattern in favour of uniform warmth. |

## Table 15
| #14 | Iteration: Deck rebuilt from scratch three times | HIGH |
| --- | --- | --- |
| Problem: The first two versions of the deck accumulated enough structural and spacing debt that iterative fixes became less efficient than a clean rebuild. This happened at v3 and v5. User action: User said: "start PDF from scratch, build a grid, build the rules, follow the rules, and make a great presentation. Don't overload each page." Fix applied: v3 was a clean rebuild with grid constants (cx/cw), spacing rules (CT, CB), and a one-idea-per-slide policy. v5 added chapter structure. Each rebuild required re-implementing all prior fixes. | Problem: The first two versions of the deck accumulated enough structural and spacing debt that iterative fixes became less efficient than a clean rebuild. This happened at v3 and v5. User action: User said: "start PDF from scratch, build a grid, build the rules, follow the rules, and make a great presentation. Don't overload each page." Fix applied: v3 was a clean rebuild with grid constants (cx/cw), spacing rules (CT, CB), and a one-idea-per-slide policy. v5 added chapter structure. Each rebuild required re-implementing all prior fixes. | Problem: The first two versions of the deck accumulated enough structural and spacing debt that iterative fixes became less efficient than a clean rebuild. This happened at v3 and v5. User action: User said: "start PDF from scratch, build a grid, build the rules, follow the rules, and make a great presentation. Don't overload each page." Fix applied: v3 was a clean rebuild with grid constants (cx/cw), spacing rules (CT, CB), and a one-idea-per-slide policy. v5 added chapter structure. Each rebuild required re-implementing all prior fixes. |

## Table 16
| #15 | Content: Skills grid descriptions clipping | LOW |
| --- | --- | --- |
| Problem: Several cells in the 4×3 skills grid had description text that ran beyond the right edge of the cell (e.g. 'Palette, contrast, WCAG, dark mode' clipped at 'dark mo...'). User action: Implicit - visible in rendered output during review cycles Fix applied: Reduced cell content to short comma-separated keywords that fit within the 3-column cell width at 9pt Mono. Verified against character-width calculations. | Problem: Several cells in the 4×3 skills grid had description text that ran beyond the right edge of the cell (e.g. 'Palette, contrast, WCAG, dark mode' clipped at 'dark mo...'). User action: Implicit - visible in rendered output during review cycles Fix applied: Reduced cell content to short comma-separated keywords that fit within the 3-column cell width at 9pt Mono. Verified against character-width calculations. | Problem: Several cells in the 4×3 skills grid had description text that ran beyond the right edge of the cell (e.g. 'Palette, contrast, WCAG, dark mode' clipped at 'dark mo...'). User action: Implicit - visible in rendered output during review cycles Fix applied: Reduced cell content to short comma-separated keywords that fit within the 3-column cell width at 9pt Mono. Verified against character-width calculations. |

## Table 17
| #16 | Content: Audience tiles too small with empty bottom | MEDIUM |
| --- | --- | --- |
| Problem: Audience attribute tiles (Role, Experience, Context, Driver) were fixed at 88pt height, leaving ~200pt of empty space below the 2×2 grid in the right column. User action: User observed during visual review Fix applied: Changed tile_h to (CB − CT − 20 − 12) / 2, filling the full right column. Added supporting detail lines to each tile so the taller cards don't look empty. | Problem: Audience attribute tiles (Role, Experience, Context, Driver) were fixed at 88pt height, leaving ~200pt of empty space below the 2×2 grid in the right column. User action: User observed during visual review Fix applied: Changed tile_h to (CB − CT − 20 − 12) / 2, filling the full right column. Added supporting detail lines to each tile so the taller cards don't look empty. | Problem: Audience attribute tiles (Role, Experience, Context, Driver) were fixed at 88pt height, leaving ~200pt of empty space below the 2×2 grid in the right column. User action: User observed during visual review Fix applied: Changed tile_h to (CB − CT − 20 − 12) / 2, filling the full right column. Added supporting detail lines to each tile so the taller cards don't look empty. |
