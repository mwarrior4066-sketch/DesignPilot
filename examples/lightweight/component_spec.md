# Lightweight Example — Component Specification

Use starter pack:
- `dist/lite_starters/component_spec.md`

Then start with:
> I am starting a lightweight DesignPilot session for Component Specification. Ask only for the minimum missing context and keep the route narrow unless the task clearly needs escalation.
